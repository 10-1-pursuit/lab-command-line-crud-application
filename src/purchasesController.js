const inform = console.log
const {nanoid} = require("nanoid")

function create(purchases,purchaseName){
    const newPurchase = {
        id: nanoid(4),
        name: purchaseName,
        amount: 
        donation: 
    }
}